# Evaluation dependencies — install separately from production requirements
# pip install -r requirements-eval.txt

ragas==0.1.21
pytest==8.3.5
pytest-asyncio==0.24.0
httpx==0.27.0
pandas==2.2.3
datasets==3.1.0

# Optional: for Claude or OpenAI as judge LLM in generate_testset.py / evaluate_e2e.py
# langchain-anthropic
# langchain-openai
